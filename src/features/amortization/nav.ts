import { Calculator } from "lucide-react";

// Sidebar registration for this feature. Removing the feature = delete this
// folder, then drop the import and spread in src/components/Sidebar.tsx.
export const amortizationNavItem = {
  to: "/amortization",
  label: "Amortization",
  icon: Calculator,
};
