/**
 * Destinations for the public marketing nav.
 *
 * Sign in points at the Circle community login rather than the native
 * /login route. To revert, set SIGN_IN_URL back to "/login" — the native
 * auth routes (/login, /signup, /reset-password) are untouched and still work.
 */
export const SIGN_IN_URL =
  "https://login.circle.so/sign_in?request_host=community.amplificawealth.com#email";
